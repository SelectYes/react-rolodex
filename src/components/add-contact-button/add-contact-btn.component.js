import React from 'react';
import "./add-contact-btn.css";

export const AddContactBtn = (props) => {
    return (
        <div>
            <button className="add-contact-btn">Add Contact</button>
        </div>
    )
}